apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hallowelt
spec:
  rules:
  - host: hallowelt.priv
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hallowelt-nodeport
            port:
              number: 80
